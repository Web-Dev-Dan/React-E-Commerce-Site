import React from 'react';

const CategoriesBox = ({ key, title, img }) => {
    return (
        <div className="categories-box">
            <h1>{title}</h1>
        </div>
    )
}

export default CategoriesBox;